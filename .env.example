# GTEC CTL-Flow Environment Configuration

# Server Port
PORT=5000

# Node Environment: 'development' or 'production'
NODE_ENV=development

# Database path (defaults to ./database.sqlite in root if not set)
# For Docker deployments, set to /app/data/database.sqlite
DATABASE_PATH=./database.sqlite
